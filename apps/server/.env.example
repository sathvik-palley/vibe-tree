# Server configuration
PORT=3002
HOST=0.0.0.0

# Default project path
PROJECT_PATH=

# Auto-load projects (comma-separated paths)
DEFAULT_PROJECTS=

# JWT secret for authentication
JWT_SECRET=your-jwt-secret-change-in-production

# Authentication for web app (set AUTH_REQUIRED=true to enable login)
AUTH_REQUIRED=false
USERNAME=admin
PASSWORD=password123